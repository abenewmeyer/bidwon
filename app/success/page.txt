import Link from 'next/link';
import { CheckCircle, ArrowRight } from 'lucide-react';

export default function SuccessPage() {
  return (
    <div className="min-h-screen bg-slate-950 flex items-center justify-center px-6">
      <div className="max-w-md w-full bg-slate-900 border border-slate-800 rounded-3xl p-8 text-center shadow-2xl">
        <div className="flex justify-center mb-6">
          <CheckCircle className="h-16 w-16 text-emerald-400" />
        </div>
        <h1 className="text-3xl font-bold text-white mb-4">Payment Secured.</h1>
        <p className="text-slate-400 mb-8">
          Your BidWon Command Center is now being provisioned with the Strategic Vector Vault. 
        </p>
        <Link 
          href="/dashboard" 
          className="flex items-center justify-center w-full bg-indigo-500 hover:bg-indigo-400 text-white font-semibold py-3 px-6 rounded-xl transition-all"
        >
          Enter Command Center <ArrowRight className="ml-2 h-5 w-5" />
        </Link>
      </div>
    </div>
  );
}